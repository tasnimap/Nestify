@page "/register-old"
@* Redirected — the login and register pages are now unified at /auth *@
@inject NavigationManager Nav
@code { protected override void OnInitialized() => Nav.NavigateTo("/auth", replace: true); }
